/submit/expense
    POST, returns a JSON
    inputs : date (optional), text, amount, user
    output : status = ok

************************************************************************

/submit/income
    POST, returns a JSON
    inputs : date (optional), text, amount, user
    output : status = ok